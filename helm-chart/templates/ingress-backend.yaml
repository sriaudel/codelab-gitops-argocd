apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-backend
spec:
  tls:
  - hosts:
    - rkaeffer-kcl-backend.codelab.cloud-sp.eu
  rules:
    - host: rkaeffer-kcl-backend.codelab.cloud-sp.eu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 443